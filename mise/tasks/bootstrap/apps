#!/usr/bin/env bash
#MISE description="[bootstrap] Sync apps via helmfile"
set -euo pipefail

BOOTSTRAP_DIR="$(dirname "$0")/../../../clusters/psb/bootstrap"

helmfile -f "${BOOTSTRAP_DIR}/helmfile.d/01-apps.yaml" sync --hide-notes
