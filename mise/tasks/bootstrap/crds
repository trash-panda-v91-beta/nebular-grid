#!/usr/bin/env bash
#MISE description="[bootstrap] Apply CRDs via helmfile"
set -euo pipefail

BOOTSTRAP_DIR="$(dirname "$0")/../../../clusters/psb/bootstrap"

helmfile -f "${BOOTSTRAP_DIR}/helmfile.d/00-crds.yaml" template -q | kubectl apply --server-side -f -
