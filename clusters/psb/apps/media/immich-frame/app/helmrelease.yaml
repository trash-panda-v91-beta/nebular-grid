---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: immich-frame
spec:
  chartRef:
    kind: OCIRepository
    name: immich-frame
  interval: 1h
  values:
    controllers:
      immich-frame:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/immichframe/immichframe
              tag: v1.0.35.0@sha256:6ff37b0b1f94822e6ed006315f7b517e300d6a4c26881a6701261b3de4b69de9
            env:
              TZ: Europe/Prague
              IMMICHFRAME_CONFIG_PATH: /config
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - ALL
              readOnlyRootFilesystem: true
            resources:
              requests:
                cpu: 10m
              limits:
                memory: 256Mi

    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch

    service:
      app:
        controller: immich-frame
        ports:
          http:
            port: &port 8080

    persistence:
      config:
        type: secret
        name: immich-frame-secret
        globalMounts:
          - path: /config/Settings.yml
            subPath: Settings.yml
            readOnly: true

    route:
      app:
        hostnames:
          - immich-frame.nebular-grid.space
        parentRefs:
          - name: envoy-internal
            namespace: network
        rules:
          - backendRefs:
              - identifier: app
                port: *port
